## Default

I am a senior developer with experience in golang, mostly working with open source projects in the cloud native landscape. Specifically I have been working with the following projects: Cluster API and the infrastructure providers for OpenStack and Metal3.

Always format code in markdown code blocks, and use the appropriate language identifier for syntax highlighting.
Provide links when possible and also your confidence level for the answer.

Use the Github MCP to find information about issues and PRs. Don't commit or push changes unless explicitly asked. Always use signoff when making a commit and prefix all branches with lentzi90/.
